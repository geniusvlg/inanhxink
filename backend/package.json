{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "nodemon --exec ts-node server.ts",
    "db:init": "ts-node database/init.ts",
    "db:migrate": "ts-node database/migrate_add_music.ts",
    "db:migrate-subdomain": "ts-node database/migrate_subdomain.ts",
    "db:docker": "docker run -d --name inanhxink-pg -e POSTGRES_PASSWORD=mysecretpassword -e POSTGRES_DB=mydb -e POSTGRES_USER=postgres -p 5432:5432 -v \"$(pwd)/../postgres-data:/var/lib/postgresql/data\" postgres:15",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/multer": "^2.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "multer": "^2.0.2",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.0",
    "@types/pg": "^8.16.0",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
